# 🔐 Excel Password Unlocker

โปรแกรมสำหรับปลด password ไฟล์ Excel ที่ลืมรหัสผ่าน เขียนด้วย Python

## 📋 คุณสมบัติ

- ✅ ปลดล็อค **Sheet Protection** (การล็อคเซลล์/แผ่นงาน) - **ไม่ต้องรู้ password**
- ✅ ปลดล็อค **File Protection** (ต้องใส่รหัสเปิดไฟล์) - **ต้องรู้ password**
- ✅ รองรับไฟล์ Excel 2007+ (`.xlsx`)
- ✅ ใช้งานง่าย มี Interface ภาษาไทย
- ✅ แสดงผลลัพธ์ชัดเจน

## 🔍 ความแตกต่างของการล็อค Excel

### 1. Sheet Protection (การล็อคเซลล์)
- **อาการ**: เปิดไฟล์ได้ปกติ แต่แก้ไขเซลล์ไม่ได้
- **วิธีปลดล็อค**: ใช้โปรแกรมนี้ **ไม่ต้องรู้ password** ✨

### 2. File Protection (การล็อคไฟล์)
- **อาการ**: ต้องใส่ password ถึงจะเปิดไฟล์ได้
- **วิธีปลดล็อค**: ต้อง**รู้ password** จึงจะปลดล็อคได้

## 🚀 การติดตั้ง

### 1. ติดตั้ง Python
ตรวจสอบว่ามี Python 3.7+ แล้ว:
```bash
python --version
```

### 2. Clone Repository
```bash
git clone https://github.com/yotsawan-s/TestUnlock.git
cd TestUnlock
```

### 3. ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

## 💻 วิธีใช้งาน

### วิธีที่ 1: Run แบบ Interactive
```bash
python unlock_excel.py
```
จากนั้นโปรแกรมจะถามชื่อไฟล์และวิธีการปลดล็อคที่ต้องการ

### วิธีที่ 2: ระบุไฟล์ผ่าน Command Line
```bash
python unlock_excel.py locked_file.xlsx
```

## 📝 ตัวอย่างการใช้งาน

### กรณีที่ 1: ปลดล็อค Sheet Protection (ไม่รู้ password)

1. วางไฟล์ Excel ที่ล็อคไว้ในโฟลเดอร์เดียวกับ `unlock_excel.py`
2. รันโปรแกรม: 
   ```bash
   python unlock_excel.py myfile.xlsx
   ```
3. เลือก **วิธีที่ 1** หรือ **3**
4. โปรแกรมจะสร้างไฟล์ `unlocked_myfile.xlsx` ที่ปลดล็อคแล้ว

### กรณีที่ 2: ปลดล็อค File Protection (รู้ password)

1. วางไฟล์ Excel ที่ล็อคไว้ในโฟลเดอร์เดียวกับ `unlock_excel.py`
2. รันโปรแกรม:
   ```bash
   python unlock_excel.py myfile.xlsx
   ```
3. เลือก **วิธีที่ 2** หรือ **3**
4. ใส่ password ที่รู้
5. โปรแกรมจะสร้างไฟล์ `unlocked_myfile.xlsx` ที่ปลดล็อคแล้ว

## 📂 โครงสร้างโปรเจค

```
TestUnlock/
├── unlock_excel.py      # สคริปต์หลัก
├── requirements. txt     # Python dependencies
├── README. md           # คู่มือนี้
├── . gitignore         # ไฟล์ที่ไม่ต้อง commit
└── (วางไฟล์ Excel ที่ต้องการปลดล็อคที่นี่)
```

## 🧪 การทดสอบอัตโนมัติ (GitHub Actions)

โปรเจคนี้มี GitHub Actions workflow ที่จะทดสอบการทำงานอัตโนมัติ:

- ✅ ทดสอบการติดตั้ง dependencies
- ✅ สร้างไฟล์ Excel ทดสอบที่มี Sheet Protection
- ✅ ทดสอบการปลดล็อค Sheet Protection
- ✅ ตรวจสอบความถูกต้องของผลลัพธ์

### ดูผลการทดสอบ

1. ไปที่หน้า **Actions** ใน repository
2. คลิกที่ workflow run ล่าสุด
3. ดาวน์โหลด **Artifacts** เพื่อดูไฟล์ทดสอบ

### รัน Workflow ด้วยตัวเอง

1. ไปที่ **Actions** &gt; **Test Excel Unlocker**
2. คลิก **Run workflow**
3. เลือก branch และคลิก **Run workflow**

## 📁 โฟลเดอร์ทดสอบ

ใช้โฟลเดอร์ `test_samples/` สำหรับวางไฟล์ Excel ที่ต้องการทดสอบ:

```bash
# วางไฟล์ที่ต้องการทดสอบ
cp your_locked_file.xlsx test_samples/

# รันการทดสอบ
python unlock_excel.py test_samples/your_locked_file.xlsx

# ตรวจสอบผลลัพธ์
ls test_samples/unlocked_*
```

**หมายเหตุ**: ไฟล์ Excel จะไม่ถูก commit ขึ้น GitHub (ตาม .gitignore)

## ⚠️ ข้อควรระวัง

1. **ใช้กับไฟล์ของตัวเองเท่านั้น**: โปรแกรมนี้สร้างขึ้นเพื่อช่วยเหลือในกรณีที่ลืม password ไฟล์ของตนเอง
2. **ข้อจำกัด**: File Protection ที่แข็งแรงไม่สามารถปลดล็อคได้ถ้าไม่รู้ password
3. **สำรองข้อมูล**: แนะนำให้สำรองไฟล์ต้นฉบับก่อนใช้งานเสมอ
4. **ไม่มี Virus/Malware**: โค้ดทั้งหมดเปิดให้ตรวจสอบได้ 100%

## 📚 เทคโนโลยีที่ใช้

- **Python 3.7+**
- **msoffcrypto-tool**:  ไลบรารีสำหรับจัดการไฟล์ Office ที่เข้ารหัส
- **openpyxl**: ไลบรารีสำหรับจัดการไฟล์ Excel

## 🤝 การมีส่วนร่วม

ยินดีรับ Pull Request และ Issue! หากมีปัญหาหรือข้อเสนอแนะ

## 📄 License

โปรเจคนี้เปิดให้ใช้งานฟรีเพื่อการศึกษาและใช้งานส่วนตัว

## ⚖️ ข้อจำกัดความรับผิดชอบ

- การใช้โปรแกรมนี้เพื่อปลด password ไฟล์ที่ไม่ใช่ของคุณอาจผิดกฎหมาย
- ผู้พัฒนาไม่รับผิดชอบต่อการใช้งานที่ผิดวัตถุประสงค์
- ใช้งานโดยคำนึงถึงจริยธรรมและกฎหมาย

---

💡 **คำแนะนำ**:  หากปลดล็อคไม่สำเร็จ ให้ตรวจสอบว่าไฟล์ใช้การล็อคแบบไหน และมี password หรือไม่
