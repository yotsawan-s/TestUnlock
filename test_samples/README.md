# 📁 Test Samples

โฟลเดอร์นี้ใช้สำหรับเก็บไฟล์ Excel ที่ต้องการทดสอบการปลดล็อค

## 📝 วิธีใช้งาน

1. **วางไฟล์ Excel ที่ต้องการทดสอบ**ในโฟลเดอร์นี้
2. **รันโปรแกรม**:
   ```bash
   python unlock_excel.py test_samples/your_file.xlsx
   ```
3. **ตรวจสอบผลลัพธ์**: ไฟล์ที่ปลดล็อคแล้วจะถูกบันทึกในโฟลเดอร์เดียวกัน

## 🧪 GitHub Actions

GitHub Actions จะสร้างไฟล์ทดสอบอัตโนมัติเมื่อมีการ push หรือ pull request:
- **locked_test.xlsx** - ไฟล์ที่มี Sheet Protection
- **unlocked_test.xlsx** - ไฟล์ที่ปลดล็อคแล้ว

คุณสามารถดาวน์โหลดไฟล์เหล่านี้จากหน้า Actions > Artifacts

## ⚠️ หมายเหตุ

- ไฟล์ Excel (*.xlsx, *.xls) จะไม่ถูก commit ขึ้น GitHub (ตาม .gitignore)
- วางเฉพาะไฟล์ทดสอบที่ไม่มีข้อมูลสำคัญ
- สำหรับไฟล์จริง ให้ใช้ในเครื่องของคุณเองเท่านั้น

## 📋 ตัวอย่างการสร้างไฟล์ทดสอบ

ใช้ Python สร้างไฟล์ Excel ที่มี Sheet Protection:

```python
from openpyxl import Workbook

wb = Workbook()
ws = wb.active
ws.title = "ทดสอบ"

# เพิ่มข้อมูล
ws['A1'] = 'ข้อมูลทดสอบ'

# ล็อค sheet
ws.protection.sheet = True
ws.protection.password = 'test123'

# บันทึก
wb.save('test_samples/my_locked_file.xlsx')
```

จากนั้นทดสอบการปลดล็อค:
```bash
python unlock_excel.py test_samples/my_locked_file.xlsx
```
